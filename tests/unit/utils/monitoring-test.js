import monitoring from 'ember-dbmon/utils/monitoring';
import { module, test } from 'qunit';

module('Unit | Utility | monitoring');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = monitoring();
  assert.ok(result);
});

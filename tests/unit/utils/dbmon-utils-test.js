import dbmonUtils from 'ember-dbmon/utils/dbmon-utils';
import { module, test } from 'qunit';

module('Unit | Utility | dbmon utils');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = dbmonUtils();
  assert.ok(result);
});

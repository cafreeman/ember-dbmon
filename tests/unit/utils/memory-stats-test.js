import memoryStats from 'ember-dbmon/utils/memory-stats';
import { module, test } from 'qunit';

module('Unit | Utility | memory stats');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = memoryStats();
  assert.ok(result);
});
